import { ThbsbackgroundDirective } from './thbsbackground.directive';

describe('ThbsbackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new ThbsbackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
